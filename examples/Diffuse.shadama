program "Diffuse"

patch D (g, r, b, a)
breed Filler (x, y)
  
def fill(field) {
  field.r = 1.0;
  field.g = 0.0;
  field.b = 0.0;
  field.a = 1.0;
}

static setup() {
  Filler.fillSpace("x", "y", width, height);
  Filler.fill(D);
}

static loop() {
  D.diffuse("r");
  D.draw();
}
